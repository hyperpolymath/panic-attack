# SPDX-License-Identifier: PMPL-1.0-or-later
# panic-attack shell completions for nushell
# Generated by: panic-attack completions nushell
# Author: Jonathan D.A. Jewell <j.d.a.jewell@open.ac.uk>

module completions {

  def "nu-complete panic-attack report_view" [] {
    [ "summary" "accordion" "dashboard" "matrix" ]
  }

  def "nu-complete panic-attack output_format" [] {
    [ "json" "yaml" "nickel" "sarif" ]
  }

  # Universal stress testing and logic-based bug signature detection
  export extern panic-attack [
    --report-view: string@"nu-complete panic-attack report_view"
    --expand-sections
    --output-format: string@"nu-complete panic-attack output_format"
    --pivot
    --store: path
    --quiet
    --parallel
    --help(-h)                # Print help
    --version(-V)             # Print version
  ]

  def "nu-complete panic-attack assail report_view" [] {
    [ "summary" "accordion" "dashboard" "matrix" ]
  }

  def "nu-complete panic-attack assail output_format" [] {
    [ "json" "yaml" "nickel" "sarif" ]
  }

  # Run assail static analysis on a target program
  export extern "panic-attack assail" [
    --output(-o): path        # Output report to file
    --verbose(-v)             # Verbose output
    --attest                  # Enable attestation chain (writes .attestation.json sidecar)
    --signing-key: path       # Path to Ed25519 private key (32-byte seed) for signing the attestation. Requires the `signing` feature
    --logtalk: path           # Export kanren logic facts as Logtalk predicates for hypatia integration
    --report-view: string@"nu-complete panic-attack assail report_view"
    --expand-sections
    --output-format: string@"nu-complete panic-attack assail output_format"
    --pivot
    --store: path
    --quiet
    --parallel
    --help(-h)                # Print help
    target: path              # Target program or directory to analyze
  ]

  def "nu-complete panic-attack attack probe" [] {
    [ "auto" "always" "never" ]
  }

  def "nu-complete panic-attack attack axis" [] {
    [ "cpu" "memory" "disk" "network" "concurrency" "time" ]
  }

  def "nu-complete panic-attack attack intensity" [] {
    [ "light" "medium" "heavy" "extreme" ]
  }

  def "nu-complete panic-attack attack report_view" [] {
    [ "summary" "accordion" "dashboard" "matrix" ]
  }

  def "nu-complete panic-attack attack output_format" [] {
    [ "json" "yaml" "nickel" "sarif" ]
  }

  # Execute a single attack on a target program
  export extern "panic-attack attack" [
    --profile: path           # Attack profile file (json/yaml)
    --arg: string             # Extra argument(s) passed to the target program
    --axis-arg: string        # Axis-specific argument, format: AXIS=ARG
    --probe: string@"nu-complete panic-attack attack probe" # Probe mode for detecting unsupported flags
    --axis(-a): string@"nu-complete panic-attack attack axis" # Attack axis to use
    --intensity(-i): string@"nu-complete panic-attack attack intensity" # Attack intensity
    --duration(-d): string    # Attack duration in seconds
    --report-view: string@"nu-complete panic-attack attack report_view"
    --expand-sections
    --output-format: string@"nu-complete panic-attack attack output_format"
    --pivot
    --store: path
    --quiet
    --parallel
    --help(-h)                # Print help
    program: path             # Target program to attack
  ]

  def "nu-complete panic-attack assault probe" [] {
    [ "auto" "always" "never" ]
  }

  def "nu-complete panic-attack assault axes" [] {
    [ "cpu" "memory" "disk" "network" "concurrency" "time" ]
  }

  def "nu-complete panic-attack assault intensity" [] {
    [ "light" "medium" "heavy" "extreme" ]
  }

  def "nu-complete panic-attack assault report_view" [] {
    [ "summary" "accordion" "dashboard" "matrix" ]
  }

  def "nu-complete panic-attack assault output_format" [] {
    [ "json" "yaml" "nickel" "sarif" ]
  }

  # Full assault: combines static analysis (`assail`) with multi-axis dynamic attacks (`attack`)
  export extern "panic-attack assault" [
    --source: path            # Source directory or file for assail analysis (defaults to PROGRAM)
    --profile: path           # Attack profile file (json/yaml)
    --arg: string             # Extra argument(s) passed to the target program
    --axis-arg: string        # Axis-specific argument, format: AXIS=ARG
    --probe: string@"nu-complete panic-attack assault probe" # Probe mode for detecting unsupported flags
    --axes(-a): string@"nu-complete panic-attack assault axes" # Attack axes (default: all)
    --intensity(-i): string@"nu-complete panic-attack assault intensity" # Attack intensity
    --duration(-d): string    # Attack duration per axis in seconds
    --output(-o): path        # Output report to file
    --report-view: string@"nu-complete panic-attack assault report_view"
    --expand-sections
    --output-format: string@"nu-complete panic-attack assault output_format"
    --pivot
    --store: path
    --quiet
    --parallel
    --help(-h)                # Print help
    program: path             # Target program to assault
  ]

  def "nu-complete panic-attack ambush axes" [] {
    [ "cpu" "memory" "disk" "network" "concurrency" "time" ]
  }

  def "nu-complete panic-attack ambush intensity" [] {
    [ "light" "medium" "heavy" "extreme" ]
  }

  def "nu-complete panic-attack ambush report_view" [] {
    [ "summary" "accordion" "dashboard" "matrix" ]
  }

  def "nu-complete panic-attack ambush output_format" [] {
    [ "json" "yaml" "nickel" "sarif" ]
  }

  # Ambush: run a target program while applying ambient stressors
  export extern "panic-attack ambush" [
    --source: path            # Source directory or file for assail analysis (defaults to PROGRAM)
    --timeline: path          # Timeline file (JSON/YAML) for DAW-style scheduling
    --profile: path           # Attack profile file (json/yaml) for target args
    --arg: string             # Extra argument(s) passed to the target program
    --axis-arg: string        # Axis-specific argument, format: AXIS=ARG
    --axes(-a): string@"nu-complete panic-attack ambush axes" # Stress axes to apply (default: all)
    --intensity(-i): string@"nu-complete panic-attack ambush intensity" # Stress intensity
    --duration(-d): string    # Ambush duration per axis in seconds
    --output(-o): path        # Output report to file
    --report-view: string@"nu-complete panic-attack ambush report_view"
    --expand-sections
    --output-format: string@"nu-complete panic-attack ambush output_format"
    --pivot
    --store: path
    --quiet
    --parallel
    --help(-h)                # Print help
    program: path             # Target program to ambush
  ]

  def "nu-complete panic-attack amuck preset" [] {
    [ "light" "dangerous" ]
  }

  def "nu-complete panic-attack amuck report_view" [] {
    [ "summary" "accordion" "dashboard" "matrix" ]
  }

  def "nu-complete panic-attack amuck output_format" [] {
    [ "json" "yaml" "nickel" "sarif" ]
  }

  # Amuck: mutate a file with dangerous/user-defined combinations and optionally execute checks
  export extern "panic-attack amuck" [
    --preset: string@"nu-complete panic-attack amuck preset" # Mutation preset when no --spec is provided
    --spec: path              # Custom mutation combinations file (json/yaml)
    --max-combinations: string # Maximum combinations to execute
    --output-dir: path        # Directory where mutated variants are written
    --exec-program: string    # Optional executable run per mutated file
    --exec-arg: string        # Arguments for --exec-program; use {file} for the mutated file path
    --output(-o): path        # Optional report output path (JSON)
    --report-view: string@"nu-complete panic-attack amuck report_view"
    --expand-sections
    --output-format: string@"nu-complete panic-attack amuck output_format"
    --pivot
    --store: path
    --quiet
    --parallel
    --help(-h)                # Print help
    target: path              # Target file to mutate (never modified in place)
  ]

  def "nu-complete panic-attack abduct scope" [] {
    [ "none" "direct" "two-hops" "directory" ]
  }

  def "nu-complete panic-attack abduct time_mode" [] {
    [ "normal" "frozen" "slow" ]
  }

  def "nu-complete panic-attack abduct report_view" [] {
    [ "summary" "accordion" "dashboard" "matrix" ]
  }

  def "nu-complete panic-attack abduct output_format" [] {
    [ "json" "yaml" "nickel" "sarif" ]
  }

  # Abduct: isolate, lock, and time-skew a target file (optionally with dependencies)
  export extern "panic-attack abduct" [
    --source-root: path       # Optional source root used to resolve dependency graph paths
    --scope: string@"nu-complete panic-attack abduct scope" # Dependency scope for selecting related files
    --output-dir: path        # Workspace root where abduct runs are created
    --no-lock                 # Disable readonly lock-down of copied files
    --mtime-offset-days: string # Shift copied file mtimes by this many days (negative or positive)
    --time-mode: string@"nu-complete panic-attack abduct time_mode" # Time mode metadata exported to executed process
    --time-scale: string      # Virtual time scale factor when --time-mode slow
    --virtual-now: string     # Optional virtual timestamp (RFC3339) exported as ABDUCT_VIRTUAL_NOW
    --exec-program: string    # Optional executable to run after lock/time setup
    --exec-arg: string        # Arguments for --exec-program; placeholders: {file}, {workspace}
    --exec-timeout: string    # Timeout (seconds) for the optional execution command
    --output(-o): path        # Optional report output path (JSON)
    --report-view: string@"nu-complete panic-attack abduct report_view"
    --expand-sections
    --output-format: string@"nu-complete panic-attack abduct output_format"
    --pivot
    --store: path
    --quiet
    --parallel
    --help(-h)                # Print help
    target: path              # Target file to abduct into an isolated workspace
  ]

  def "nu-complete panic-attack adjudicate report_view" [] {
    [ "summary" "accordion" "dashboard" "matrix" ]
  }

  def "nu-complete panic-attack adjudicate output_format" [] {
    [ "json" "yaml" "nickel" "sarif" ]
  }

  # Adjudicate: aggregate reports into a campaign-wide expert-system verdict
  export extern "panic-attack adjudicate" [
    --output(-o): path        # Optional report output path (JSON)
    --report-view: string@"nu-complete panic-attack adjudicate report_view"
    --expand-sections
    --output-format: string@"nu-complete panic-attack adjudicate output_format"
    --pivot
    --store: path
    --quiet
    --parallel
    --help(-h)                # Print help
    ...reports: path          # Input report files (assault/amuck/abduct JSON, assault YAML)
  ]

  def "nu-complete panic-attack axial lang" [] {
    [ "en" "es" "fr" "de" "ja" ]
  }

  def "nu-complete panic-attack axial report_view" [] {
    [ "summary" "accordion" "dashboard" "matrix" ]
  }

  def "nu-complete panic-attack axial output_format" [] {
    [ "json" "yaml" "nickel" "sarif" ]
  }

  # Axial: observe target reactions across attack axes from tool outputs and report artifacts
  export extern "panic-attack axial" [
    --exec-program: string    # Optional executable to run for reaction observation
    --exec-arg: string        # Arguments for --exec-program; placeholder: {target}
    --repeat: string          # Number of repeated observation runs for --exec-program
    --timeout: string         # Timeout (seconds) per observation run
    --report: path            # Existing reports to observe (can be provided multiple times)
    --head: string            # Include the first N lines from observed output/content
    --tail: string            # Include the last N lines from observed output/content
    --grep: string            # Exact pattern search (repeatable)
    --agrep: string           # Approximate/fuzzy pattern search (repeatable)
    --agrep-distance: string  # Maximum edit distance for --agrep matches
    --lang: string@"nu-complete panic-attack axial lang" # Output language (ISO 639-1 code: en, es, fr, de, ja)
    --aspell                  # Enable aspell checks on observed text
    --aspell-lang: string     # Aspell dictionary language (default derived from --lang)
    --markdown-output: path   # Optional markdown output path
    --pandoc-to: string       # Optional pandoc target format (e.g. html, docx, gfm, latex)
    --pandoc-output: path     # Optional pandoc output path (required for custom destination)
    --output(-o): path        # Optional report output path (JSON)
    --report-view: string@"nu-complete panic-attack axial report_view"
    --expand-sections
    --output-format: string@"nu-complete panic-attack axial output_format"
    --pivot
    --store: path
    --quiet
    --parallel
    --help(-h)                # Print help
    target: path              # Target file/program under observation
  ]

  def "nu-complete panic-attack analyze report_view" [] {
    [ "summary" "accordion" "dashboard" "matrix" ]
  }

  def "nu-complete panic-attack analyze output_format" [] {
    [ "json" "yaml" "nickel" "sarif" ]
  }

  # Analyze crash reports for bug signatures
  export extern "panic-attack analyze" [
    --report-view: string@"nu-complete panic-attack analyze report_view"
    --expand-sections
    --output-format: string@"nu-complete panic-attack analyze output_format"
    --pivot
    --store: path
    --quiet
    --parallel
    --help(-h)                # Print help
    report: path              # Crash report file (JSON)
  ]

  def "nu-complete panic-attack report report_view" [] {
    [ "summary" "accordion" "dashboard" "matrix" ]
  }

  def "nu-complete panic-attack report output_format" [] {
    [ "json" "yaml" "nickel" "sarif" ]
  }

  # Render a saved assault report with view controls
  export extern "panic-attack report" [
    --report-view: string@"nu-complete panic-attack report report_view"
    --expand-sections
    --output-format: string@"nu-complete panic-attack report output_format"
    --pivot
    --store: path
    --quiet
    --parallel
    --help(-h)                # Print help
    report: path              # JSON assault report path
  ]

  def "nu-complete panic-attack tui report_view" [] {
    [ "summary" "accordion" "dashboard" "matrix" ]
  }

  def "nu-complete panic-attack tui output_format" [] {
    [ "json" "yaml" "nickel" "sarif" ]
  }

  # Interactive review of a saved report
  export extern "panic-attack tui" [
    --report-view: string@"nu-complete panic-attack tui report_view"
    --expand-sections
    --output-format: string@"nu-complete panic-attack tui output_format"
    --pivot
    --store: path
    --quiet
    --parallel
    --help(-h)                # Print help
    report: path              # Assault report JSON file
  ]

  def "nu-complete panic-attack gui report_view" [] {
    [ "summary" "accordion" "dashboard" "matrix" ]
  }

  def "nu-complete panic-attack gui output_format" [] {
    [ "json" "yaml" "nickel" "sarif" ]
  }

  # GUI review of a saved report
  export extern "panic-attack gui" [
    --report-view: string@"nu-complete panic-attack gui report_view"
    --expand-sections
    --output-format: string@"nu-complete panic-attack gui output_format"
    --pivot
    --store: path
    --quiet
    --parallel
    --help(-h)                # Print help
    report: path              # Assault report JSON file
  ]

  def "nu-complete panic-attack diff report_view" [] {
    [ "summary" "accordion" "dashboard" "matrix" ]
  }

  def "nu-complete panic-attack diff output_format" [] {
    [ "json" "yaml" "nickel" "sarif" ]
  }

  # Compare two assault reports (defaults to latest VerisimDB runs)
  export extern "panic-attack diff" [
    --verisimdb-dir: path     # VerisimDB directory to scan for latest reports
    --report-view: string@"nu-complete panic-attack diff report_view"
    --expand-sections
    --output-format: string@"nu-complete panic-attack diff output_format"
    --pivot
    --store: path
    --quiet
    --parallel
    --help(-h)                # Print help
    base?: path               # Base report path
    compare?: path            # Compare report path
  ]

  def "nu-complete panic-attack manifest report_view" [] {
    [ "summary" "accordion" "dashboard" "matrix" ]
  }

  def "nu-complete panic-attack manifest output_format" [] {
    [ "json" "yaml" "nickel" "sarif" ]
  }

  # Export the AI manifest as Nickel
  export extern "panic-attack manifest" [
    --path(-p): path          # Alternate AI manifest file
    --output(-o): path        # Save Nickel output to file
    --report-view: string@"nu-complete panic-attack manifest report_view"
    --expand-sections
    --output-format: string@"nu-complete panic-attack manifest output_format"
    --pivot
    --store: path
    --quiet
    --parallel
    --help(-h)                # Print help
  ]

  def "nu-complete panic-attack a2ml-export kind" [] {
    [ "assail" "attack" "assault" "ambush" "amuck" "abduct" "adjudicate" "axial" ]
  }

  def "nu-complete panic-attack a2ml-export report_view" [] {
    [ "summary" "accordion" "dashboard" "matrix" ]
  }

  def "nu-complete panic-attack a2ml-export output_format" [] {
    [ "json" "yaml" "nickel" "sarif" ]
  }

  # Export a report file into the A2ML report-bundle document type
  export extern "panic-attack a2ml-export" [
    --kind: string@"nu-complete panic-attack a2ml-export kind" # Report kind to encode in the bundle
    --output(-o): path        # Destination A2ML file
    --report-view: string@"nu-complete panic-attack a2ml-export report_view"
    --expand-sections
    --output-format: string@"nu-complete panic-attack a2ml-export output_format"
    --pivot
    --store: path
    --quiet
    --parallel
    --help(-h)                # Print help
    input: path               # Source report file (json/yaml depending on kind)
  ]

  def "nu-complete panic-attack a2ml-import kind" [] {
    [ "assail" "attack" "assault" "ambush" "amuck" "abduct" "adjudicate" "axial" ]
  }

  def "nu-complete panic-attack a2ml-import report_view" [] {
    [ "summary" "accordion" "dashboard" "matrix" ]
  }

  def "nu-complete panic-attack a2ml-import output_format" [] {
    [ "json" "yaml" "nickel" "sarif" ]
  }

  # Import an A2ML report-bundle file back into JSON
  export extern "panic-attack a2ml-import" [
    --output(-o): path        # Destination JSON file
    --kind: string@"nu-complete panic-attack a2ml-import kind" # Optional expected kind check
    --report-view: string@"nu-complete panic-attack a2ml-import report_view"
    --expand-sections
    --output-format: string@"nu-complete panic-attack a2ml-import output_format"
    --pivot
    --store: path
    --quiet
    --parallel
    --help(-h)                # Print help
    input: path               # Source A2ML bundle file
  ]

  def "nu-complete panic-attack panll report_view" [] {
    [ "summary" "accordion" "dashboard" "matrix" ]
  }

  def "nu-complete panic-attack panll output_format" [] {
    [ "json" "yaml" "nickel" "sarif" ]
  }

  # Export an assault report as a PanLL event-chain model
  export extern "panic-attack panll" [
    --output(-o): path        # Output file for PanLL export (JSON)
    --report-view: string@"nu-complete panic-attack panll report_view"
    --expand-sections
    --output-format: string@"nu-complete panic-attack panll output_format"
    --pivot
    --store: path
    --quiet
    --parallel
    --help(-h)                # Print help
    report: path              # Assault report JSON/YAML file
  ]

  def "nu-complete panic-attack help report_view" [] {
    [ "summary" "accordion" "dashboard" "matrix" ]
  }

  def "nu-complete panic-attack help output_format" [] {
    [ "json" "yaml" "nickel" "sarif" ]
  }

  # Print detailed help text (man-style)
  export extern "panic-attack help" [
    --report-view: string@"nu-complete panic-attack help report_view"
    --expand-sections
    --output-format: string@"nu-complete panic-attack help output_format"
    --pivot
    --store: path
    --quiet
    --parallel
    --help(-h)                # Print help
    command?: string          # Optional subcommand name to display help for
  ]

  def "nu-complete panic-attack assemblyline report_view" [] {
    [ "summary" "accordion" "dashboard" "matrix" ]
  }

  def "nu-complete panic-attack assemblyline output_format" [] {
    [ "json" "yaml" "nickel" "sarif" ]
  }

  # Assemblyline: batch-scan a directory of repos (assail each, aggregate results)
  export extern "panic-attack assemblyline" [
    --output(-o): path        # Output report to file
    --findings-only           # Only show repos with findings
    --min-findings: string    # Minimum number of findings to include a repo
    --incremental             # Enable incremental scanning (skip repos unchanged since last run)
    --cache: path             # Path to fingerprint cache file (default: .panic-attack-cache.json in DIRECTORY)
    --report-view: string@"nu-complete panic-attack assemblyline report_view"
    --expand-sections
    --output-format: string@"nu-complete panic-attack assemblyline output_format"
    --pivot
    --store: path
    --quiet
    --parallel
    --help(-h)                # Print help
    directory: path           # Parent directory containing repos to scan
  ]

  def "nu-complete panic-attack diagnostics report_view" [] {
    [ "summary" "accordion" "dashboard" "matrix" ]
  }

  def "nu-complete panic-attack diagnostics output_format" [] {
    [ "json" "yaml" "nickel" "sarif" ]
  }

  # Run panic-attack self-diagnostics for Hypatia/gitbot-fleet visibility
  export extern "panic-attack diagnostics" [
    --manifest: path          # Alternate AI manifest file (default: AI.a2ml)
    --report-view: string@"nu-complete panic-attack diagnostics report_view"
    --expand-sections
    --output-format: string@"nu-complete panic-attack diagnostics output_format"
    --pivot
    --store: path
    --quiet
    --parallel
    --help(-h)                # Print help
  ]

  def "nu-complete panic-attack migration-snapshot report_view" [] {
    [ "summary" "accordion" "dashboard" "matrix" ]
  }

  def "nu-complete panic-attack migration-snapshot output_format" [] {
    [ "json" "yaml" "nickel" "sarif" ]
  }

  # Take a ReScript migration snapshot (assail + migration metrics)
  export extern "panic-attack migration-snapshot" [
    --label: string           # Label for this snapshot (e.g. "before", "after", "v12-trial")
    --build-time              # Measure build time (runs `rescript build`)
    --bundle-size             # Measure bundle size (scans output directory)
    --store                   # Store snapshot as VeriSimDB hexad
    --output(-o): path        # Output snapshot to file
    --report-view: string@"nu-complete panic-attack migration-snapshot report_view"
    --expand-sections
    --output-format: string@"nu-complete panic-attack migration-snapshot output_format"
    --pivot
    --quiet
    --parallel
    --help(-h)                # Print help
    target: path              # Target ReScript project directory
  ]

  def "nu-complete panic-attack migration-diff format" [] {
    [ "markdown" "json" ]
  }

  def "nu-complete panic-attack migration-diff report_view" [] {
    [ "summary" "accordion" "dashboard" "matrix" ]
  }

  def "nu-complete panic-attack migration-diff output_format" [] {
    [ "json" "yaml" "nickel" "sarif" ]
  }

  # Compare two migration snapshots and produce a diff report
  export extern "panic-attack migration-diff" [
    --output(-o): path        # Output diff report to file
    --format: string@"nu-complete panic-attack migration-diff format" # Output format (markdown or json)
    --report-view: string@"nu-complete panic-attack migration-diff report_view"
    --expand-sections
    --output-format: string@"nu-complete panic-attack migration-diff output_format"
    --pivot
    --store: path
    --quiet
    --parallel
    --help(-h)                # Print help
    before: path              # Before snapshot JSON file
    after: path               # After snapshot JSON file
  ]

  def "nu-complete panic-attack notify report_view" [] {
    [ "summary" "accordion" "dashboard" "matrix" ]
  }

  def "nu-complete panic-attack notify output_format" [] {
    [ "json" "yaml" "nickel" "sarif" ]
  }

  # Notify: generate annotated findings summary from an assemblyline report
  export extern "panic-attack notify" [
    --output(-o): path        # Output markdown file
    --critical-only           # Only include repos with critical findings
    --min-findings: string    # Minimum findings to include a repo
    --create-issues           # Create GitHub issues for repos with critical findings (requires gh CLI)
    --github-owner: string    # GitHub owner for issue creation
    --report-view: string@"nu-complete panic-attack notify report_view"
    --expand-sections
    --output-format: string@"nu-complete panic-attack notify output_format"
    --pivot
    --store: path
    --quiet
    --parallel
    --help(-h)                # Print help
    report: path              # Assemblyline JSON report file
  ]

  def "nu-complete panic-attack image report_view" [] {
    [ "summary" "accordion" "dashboard" "matrix" ]
  }

  def "nu-complete panic-attack image output_format" [] {
    [ "json" "yaml" "nickel" "sarif" ]
  }

  # Image: generate a system health image from an assemblyline scan (fNIRS-style)
  export extern "panic-attack image" [
    --output(-o): path        # Output file for the system image JSON
    --incremental             # Enable incremental scanning
    --cache: path             # Fingerprint cache file
    --snapshot                # Take a temporal snapshot (writes to VeriSimDB)
    --label: string           # Label for the temporal snapshot
    --verisimdb-dir: path     # VeriSimDB directory for snapshots
    --panll                   # Export PanLL-format system image alongside raw output
    --report-view: string@"nu-complete panic-attack image report_view"
    --expand-sections
    --output-format: string@"nu-complete panic-attack image output_format"
    --pivot
    --store: path
    --quiet
    --parallel
    --help(-h)                # Print help
    directory: path           # Parent directory containing repos to scan (runs assemblyline internally)
  ]

  def "nu-complete panic-attack temporal report_view" [] {
    [ "summary" "accordion" "dashboard" "matrix" ]
  }

  def "nu-complete panic-attack temporal output_format" [] {
    [ "json" "yaml" "nickel" "sarif" ]
  }

  # Temporal: navigate system health through time (diff, list, replay)
  export extern "panic-attack temporal" [
    --report-view: string@"nu-complete panic-attack temporal report_view"
    --expand-sections
    --output-format: string@"nu-complete panic-attack temporal output_format"
    --pivot
    --store: path
    --quiet
    --parallel
    --help(-h)                # Print help
  ]

  def "nu-complete panic-attack temporal list report_view" [] {
    [ "summary" "accordion" "dashboard" "matrix" ]
  }

  def "nu-complete panic-attack temporal list output_format" [] {
    [ "json" "yaml" "nickel" "sarif" ]
  }

  # List all temporal snapshots
  export extern "panic-attack temporal list" [
    --verisimdb-dir: path     # VeriSimDB directory
    --report-view: string@"nu-complete panic-attack temporal list report_view"
    --expand-sections
    --output-format: string@"nu-complete panic-attack temporal list output_format"
    --pivot
    --store: path
    --quiet
    --parallel
    --help(-h)                # Print help
  ]

  def "nu-complete panic-attack temporal diff report_view" [] {
    [ "summary" "accordion" "dashboard" "matrix" ]
  }

  def "nu-complete panic-attack temporal diff output_format" [] {
    [ "json" "yaml" "nickel" "sarif" ]
  }

  # Diff two temporal snapshots
  export extern "panic-attack temporal diff" [
    --verisimdb-dir: path     # VeriSimDB directory
    --output(-o): path        # Output diff report to file
    --panll                   # Export PanLL-format temporal diff alongside raw output
    --report-view: string@"nu-complete panic-attack temporal diff report_view"
    --expand-sections
    --output-format: string@"nu-complete panic-attack temporal diff output_format"
    --pivot
    --store: path
    --quiet
    --parallel
    --help(-h)                # Print help
    from_seq: string          # Sequence number of the older snapshot
    to_seq: string            # Sequence number of the newer snapshot
  ]

  def "nu-complete panic-attack completions shell" [] {
    [ "bash" "zsh" "fish" "nushell" "powershell" ]
  }

  def "nu-complete panic-attack completions report_view" [] {
    [ "summary" "accordion" "dashboard" "matrix" ]
  }

  def "nu-complete panic-attack completions output_format" [] {
    [ "json" "yaml" "nickel" "sarif" ]
  }

  # Generate shell completions for the specified shell
  export extern "panic-attack completions" [
    --report-view: string@"nu-complete panic-attack completions report_view"
    --expand-sections
    --output-format: string@"nu-complete panic-attack completions output_format"
    --pivot
    --store: path
    --quiet
    --parallel
    --help(-h)                # Print help
    shell: string@"nu-complete panic-attack completions shell" # Shell to generate completions for
  ]

}

export use completions *
